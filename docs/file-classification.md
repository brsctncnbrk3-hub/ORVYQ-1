# Phase 0 — File Classification

Every file/directory inspected in the golden repo, classified as one of: **canonical source**,
**generated derived output**, **QA report**, **runtime cache**, **diagnostic artifact**,
**temporary proof-only file**, **obsolete file**. Source: direct inspection of
`brsctncnbrk-ops/YouTube_pepline` at `9affbd2494d8197a564c4a552b879fadb0e14a4a`.

Classification determines migration handling in later phases:
canonical source → recovered into the new repo's `templates/`/`schemas`/`scripts`/`projects`;
generated derived output → never hand-copied, always regenerated by the canonical pipeline;
QA report → never hand-copied, always regenerated by the QA chain;
runtime cache / diagnostic artifact / temporary proof-only file / obsolete file → **not
imported**.

## 1. `direction/`

| File | Classification | Notes |
|---|---|---|
| `cinematic_proof_cut.json` | canonical source | Human-authored 150s assembly recipe (compact override list into `motion_hook.json`/`proof_preview_cut.json` plus inline contextual-footage shots). This is what actually produced the shipped proof. |
| `edit_plan.json` | generated derived output | Fully resolved 109-shot, frame-accurate plan; written by `orvyq_preview_plan.mjs`/`orvyq_edit_plan.mjs`. Byte-identical copy also exists at `remotion/render_ready_project/src/data/edit_plan.json` (confirmed via diff) — that's the build-time copy, not a stale duplicate. |
| `editorial_blueprint.json` | canonical source | Master policy/rules document — every numeric QA threshold (`max_uses_per_source`, fraction caps, `max_shot_seconds`, etc.) is read from here at runtime, not hardcoded in scripts. Single source of truth for editorial constants. |
| `editorial_pause_map.json` | canonical source | The 4 approved editorial pauses (23.74s/5s, 52.62s/7s, 93.4s/6s, 126.56s/8s) plus `full_film_pause_anchors` for the eventual full film. |
| `motion_hook.json` | canonical source | The 4 opening motion-hook shots; the only place `hook_footage:true` is declared. |
| `music_cue_sheet.json` | canonical source (proof section) / planning placeholder (`full_cues`) | `proof_score` (0–150s, 5 states) is realized and canonical; `full_cues` (9 entries) are all `status: "spec_ready_asset_pending"` — specified but not yet produced. |
| `proof_preview_cut.json` | canonical source / **superseded predecessor** | An earlier, stricter 120s zero-footage proof design (`forbidden_asset_prefixes: ["assets/footage/"]`). Superseded by the 150s cinematic cut. Not the source of the approved proof — keep for historical reference only, do not treat as canonical for parity work. |
| `cinematic_revision_plan.json` | canonical source | The human-approval record (`approval_status: "user_approved"`) that authorized the pivot from the 120s zero-footage proof to the 150s cinematic-with-footage proof; its `proof_targets` numbers are exactly what `orvyq_semantic_visual_audit.mjs` enforces. |
| `direction_plan.md` | canonical source | Prose direction notes. |

## 2. `research/`

| File | Classification | Notes |
|---|---|---|
| `primary_evidence_manifest.json` | canonical source | Fetch spec for 12 official primary-source captures; consumed by `orvyq_fetch_primary_evidence.mjs`. |
| `evidence_asset_manifest.json` | canonical source | Superset spec (16 entries) including 4 originally-authored derived graphics beyond the 12 official captures. |
| `evidence_map.json` | canonical source | Master claim/source registry (12 sources, 20 claims) gating full-render eligibility. |
| `evidence_resolutions.json` | canonical source / patch overlay | Delta layered onto `evidence_map.json` at load time via `loadResolvedEvidenceMap()`; resolves 4 blocking claims for full production, does not affect the locked proof narration. |
| `research.json`, `research.md`, `sources.md` | canonical source | Raw research corpus and citation list. |

## 3. `voice/`

| File | Classification | Notes |
|---|---|---|
| `voice_script.txt` | canonical source | Full 804.36s narration script (real ASR-measured length); proof is now a frame-prefix of the full candidate rather than a script subset. Ground truth for ASR similarity checks. |
| `narration_status.json` | QA/status gate | State-tracking file, checked via `orvyq_edit_plan_tests.mjs`'s `mode === "full"` block (`full_narration_approved` must be `true`) — see that file's current committed value for today's real state, not this note. |
| `audio_repair.json` | optional narrator-file repair, read by `orvyq_audio_mix.mjs`'s `prepareNarrator()` when present | Removed: it described a splice defect (`rotate_at_seconds: 72.12`) in a different, much shorter ~114s prior proof recording, not the current 804.36s full narration (see `narration_status.json`'s `validation_summary.notes`, which confirms the committed `final_voice.mp3` already opens and closes with the script's real lines, in order). Leaving the file in place caused `prepareNarrator()` to re-rotate an already-correct recording, corrupting narration order at render time and failing `orvyq_speech_qa.py`'s script-similarity gate. If a future narrator delivery genuinely needs this repair, re-add the file only after confirming that recording (not this one) has the splice defect it describes. |
| `voice_notes.md` | canonical source | Director/voice notes. |

## 4. `assets/`

| Path | Classification | Notes |
|---|---|---|
| `audio/final_voice.mp3`, `audio/segments/0[1-9]_*.mp3` (9 files) | canonical source | Raw narration + per-section segments, all Git LFS pointers in this checkout. |
| `audio/.merge_work/concat.txt`, `concat_corrected.txt` | **obsolete file** | Leaked local-dev scratch state; absolute paths `/opt/youtube_pipeline/...` that don't exist in CI; zero references anywhere in `scripts/`. Do not import. |
| `images/.gitkeep`, `music/.gitkeep`, `sfx/.gitkeep`, `footage/.gitkeep` | canonical source (placeholder) | Legitimate empty-dir placeholders; populated at build time. |
| `footage/scene_0XX_*.mp4` (33 files) | canonical source | Licensed stock footage, Git LFS. |
| `footage/scene_0XX_*.mp4.provenance.json` (~30 files) | canonical source | License metadata, required by `orvyq_license_audit.mjs`. |
| `footage/scene_025_...reuse.provenance.json` | canonical source | Intentional-reuse provenance variant (confirmed by shared asset-hash suffix with scene_022 — not accidental duplication). |
| `asset_manifest.json` | generated derived output | Written by `remotion_build.mjs`'s `refreshAssetManifest()`. |

## 5. `qa/`

| File | Classification | Notes |
|---|---|---|
| `footage_retrieval.md`, `render_qa.md`, `voice_qa.md`, `visual_qa.md`, `storyboard_qa.md`, `research_qa.md`, `script_qa.md` | QA report | Stage-gate sign-off docs for the FactForge pipeline's `*_qa` skills. |
| `orvyq_revision_qa.md` | QA report | Describes an earlier 109-shot editorial repair pass with LUFS/dBTP numbers. |
| `license_audit.json` | generated derived output | Written by `orvyq_license_audit.mjs`. |
| `render_request_20260716T193950+03.json` | **obsolete file** | Orphaned manual-trigger file, added in the PR #10 merge commit itself; no script/workflow reads it (`grep` confirms). |
| `editorial_revision_v2.json` | **obsolete file** | Paired with the above by `revision` name; also completely unreferenced. |
| All other `qa/*.json` (speech_transcript, evidence_coverage, evidence_asset_audit, semantic_visual_audit, pacing_audit, mobile_legibility_audit, music_cue_audit, alignment_readiness, orvyq_preview_media_qa, orvyq_brightness_repair, proof_approval) | generated derived output | Written at CI run time by the corresponding audit script; absent in this static pre-run checkout as expected. |

## 6. `footage/` (acquisition-pipeline stage, upstream of `assets/footage/`)

| File | Classification | Notes |
|---|---|---|
| `footage_manifest.json`, `source_license_registry.json` | canonical source | 33-scene manifest and 40-source license registry. |
| `human_review.json` | QA report | Human sign-off per candidate. |
| `attribution_registry.json` | canonical source | Small footage attribution index. |
| `missing_assets.json`, `acquired_assets.json` | **runtime cache** (benign but flagged) | Both embed a local-machine absolute path (`/root/factforge-footage-cleanup-audits/...`) in `last_cleanup_audit` — portability smell, currently empty/harmless content. |
| `production_candidates.json` (411 KB — largest tracked file in the project) | **runtime cache** | Raw API search-result caching, not curated content. Candidate for exclusion from clean history — regenerable, low signal. |
| `scene_asset_map.json` | generated derived output | Final scene→asset resolution. |
| `retrieval_report.md` | QA report | |

## 7. `remotion/`

| Path | Classification | Notes |
|---|---|---|
| `asset_map.json`, `scene_config.json`, `composition.json` | generated derived output | Derived from `direction/edit_plan.json` by `remotion_build.mjs derive-configs`. |
| `render_ready_project/src/*.tsx` | generated derived output, **stale** | See `docs/golden-renderer-map.md` §5 — this committed copy is out of date and non-buildable; treat as disposable, always regenerate. |
| `render_ready_project/src/data/*.json` | generated derived output | Byte-identical to their `remotion/`/`direction/` sources, except... |
| `render_ready_project/src/data/human_notes.json` | canonical source (carried-forward state) | Persisted human QA notes, re-copied verbatim across builds; contains 2 open non-blocking review items. |
| `render_ready_project/.gitignore`'s `*.corrupt-617mb` rule | **obsolete file / debris** | Codified workaround for a historical rendering-corruption bug; the *repair QA step itself* (`orvyq_brightness_repair.mjs`) is legitimate and should be kept, but this ignore-pattern framing should not be reproduced verbatim. |

## 8. `prompts/`, `style/`, `storyboard/`, project-local `scripts/`

| Path | Classification | Notes |
|---|---|---|
| `prompts/*.md`, `prompts/visual_prompts.json` (152 KB total) | canonical source / **likely obsolete** | AI-image-generation prompt library; the project pivoted to licensed footage + official evidence captures instead of AI-generated imagery. Confirm with the user before importing any of it — default to not importing. |
| `style/*.md` | canonical source | Visual style bible, camera language, color palette, etc. |
| `storyboard/storyboard.md`, `storyboard.json` | canonical source | Pre-direction shot planning, upstream of `direction/`. |
| project-local `scripts/script.md`, `script_metadata.json` | canonical source | Narration script draft stage, upstream of `voice/voice_script.txt`. |

## 9. `config/`, `schemas/`, `fact_audit/`, `packaging/`, `logs/`, `output/`

| Path | Classification | Notes |
|---|---|---|
| `config/project_config.json`, `video_config.json`, `render_config.json`, `footage_apis.json` | canonical source | Small hand/generator-set config. |
| `schemas/*.schema.json` (10 files) | canonical source | JSON Schema contracts — copied into each project at scaffold time in the FactForge system. |
| `fact_audit/fact_audit_report.md` | QA report | |
| `fact_audit/claims.json` | canonical source, **needs reconciliation** | A second, differently-schemed claim registry alongside `research/evidence_map.json`'s `claims[]`. Do not blindly duplicate both into the canonical registry — reconcile which is authoritative in Phase 2. |
| `packaging/`, `logs/`, `output/` (only `.gitkeep`) | canonical source (placeholder) | Stages not yet reached for this project (`manifest.json` confirms `pending_skills: ["packaging","final_qa"]`); `logs/`/`output/` content is correctly gitignored. |

## 10. Root-level and `.github/`

| Path | Classification | Notes |
|---|---|---|
| `manifest.json` | canonical source / status | Pipeline stage tracker (`status: "READY_FOR_RENDER"`, `current_stage: "packaging"`). |
| `.github/workflows/ci.yml` | canonical source | Generic, project-agnostic CI gate — carry forward as-is. |
| `.github/workflows/orvyq-preview.yml` | canonical source | The proof workflow to reproduce. |
| `.github/workflows/render.yml` | canonical source | Legitimate full-render workflow, correctly gated behind explicit approval inputs; never yet run to completion for this project. |
| `.github/workflows/orvyq-debug.yml` | **obsolete file** | Superseded 120s-scope debug workflow; duplicates most of `orvyq-preview.yml` with less rigor and never renders. Not carried forward. |
| `.github/workflows/orvyq-debug-trigger.txt` | **obsolete file** | Dead manual-CI-nudge placeholder referencing an unrelated commit SHA; not a real workflow file; no automation reads it. Not carried forward. |
| `.gitattributes` | canonical source | LFS filter rules for `*.mp3 *.png *.mp4 *.wav *.jpg *.jpeg` — confirmed functioning, no gaps for file types present. |
| `.gitignore` | canonical source | Standard Node/OS/env excludes plus 3 pipeline-specific rules (see `docs/source-audit.md` §6 for the corrupt-frame debris rule). |

## 11. Summary — files explicitly excluded from migration

1. `qa/render_request_20260716T193950+03.json`
2. `qa/editorial_revision_v2.json`
3. `assets/audio/.merge_work/` (both concat files)
4. `footage/production_candidates.json` (regenerate via the fetch pipeline if ever needed, don't carry the cache)
5. `.github/workflows/orvyq-debug.yml`
6. `.github/workflows/orvyq-debug-trigger.txt`
7. `remotion/render_ready_project/` in its entirety (regenerated, not migrated)
8. `prompts/` tree — pending explicit confirmation; default is exclude
9. Local-machine absolute-path fields inside `footage/missing_assets.json` / `acquired_assets.json` — the files' *structure* is fine to reproduce fresh, but do not copy these specific stale path values forward
